import React from "react";
import { Menu } from "../../components/paginaInternas/menu";

export const Home = () => {
    return (
        <div>
            <Menu ativo="ativo" />
        </div>
    );
};
